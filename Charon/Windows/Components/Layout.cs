namespace Charon.Windows.Components;

/// <summary>
/// Sizes shared by the window chrome and the components. Compact scale — the fleet board is meant to
/// be readable at a glance, not scrolled (see sketches/004-board-teal-selected).
/// </summary>
internal static class Layout
{
    public const float SidebarWidth = 148f;
    public const float HeaderHeight = 60f;
    public const float NavRowHeight = 30f;
    public const float StatTileHeight = 52f;
    public const float LiveRowHeight = 23f;
    public const float ActionButtonHeight = 25f;
    public const float RowRightMargin = 8f;
}
